<canvas style="width:100%; height: 100rpx;" canvas-id="firstCanvas"></canvas>
<!-- 当使用绝对定位时，文档流后边的 canvas 的显示层级高于前边的 canvas -->
<!-- 因为 canvas-id 与前一个 canvas 重复，该 canvas 不会显示，并会发送一个错误事件到 AppService -->
<canvas style="width: 100%; height: 300px;" canvas-id="fiirstCqwanvas" bindtap="btnclick"></canvas>
<button bindtap="btnclicksd">规划路线</button>
<button bindtap="suspended">暂停</button>
<view class='countDown'>倒计时：<text style='color:red'>{{countDownNum}}</text>s</view>